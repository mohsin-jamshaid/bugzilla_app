$("#user-<%= @user.id %>").fadeOut(300, function(){
  $(this).remove();

  $(".users").prepend("<%= j (render partial: 'project_users', locals: {user: @user}) %>")
  
  toastr.success('Project has been successfully assigned to <%= @user.name.upcase %>');
});