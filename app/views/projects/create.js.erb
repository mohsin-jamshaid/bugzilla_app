$('#exampleModalCenter').modal('hide');

if($('.projects_table >tbody >tr').length == 0){
  $('.projects').children("p:last").remove();
  $('.projects_table').show();
}

$('.projects_table').append("<%= j (render partial: 'project', locals: {project: @project}) %>")

toastr.success('Project has been created successfully');